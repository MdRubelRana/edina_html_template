{
  "name": "edina_personal_portfolio",
  "version": "1.0.0",
  "description": "1. npm init -y\r 2. npm install -D tailwindcss postcss autoprefixer\r 3. npx tailwindcss init -p",
  "main": "iindex.html",
  "scripts": {
    "dev": "postcss ./src/style.css -o ./public/output.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.7",
    "postcss": "^8.4.14",
    "tailwindcss": "^3.1.3"
  },
  "dependencies": {
    "owl.carousel": "^2.3.4",
    "postcss-cli": "^9.1.0"
  }
}
